# This Next App
NODE_ENV=example

# This Next App
NEXT_PUBLIC_BASE_URL=http://localhost:3000
NEXT_PUBLIC_BASE_API_URL=http://localhost:3000/api

# Dotnet Backend
NEXT_PUBLIC_BACKEND_URL=https://neptune-be-stag.codeswift.org
NEXT_PUBLIC_API_URL=https://neptune-be-stag.codeswift.org/api

# Upstream API used by next.config.ts rewrites (server-side only)
API_PROXY_TARGET=https://neptune-be-stag.codeswift.org/api

# Uploads go through POST /api/files (private R2). No public bucket env vars. 
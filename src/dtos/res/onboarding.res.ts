/**
 * Response `dataModel` for POST /Auth/register.
 * Swagger documents only HTTP 200 with no schema — named alias kept on purpose.
 */
export type RegisterResponse = unknown; // NOSONAR — intentional named Response for unknown API body

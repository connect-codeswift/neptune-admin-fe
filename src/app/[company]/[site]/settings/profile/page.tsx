import { ProfileSettingsClient } from "@/components/settings/ProfileSettingsClient";

/** Settings — profile tab. Your photo, name and contact details. */
export default function OrgSiteProfileSettingsRoute() {
  return <ProfileSettingsClient />;
}

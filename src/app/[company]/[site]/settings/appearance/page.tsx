import { AppearanceSettingsClient } from "@/components/settings/AppearanceSettingsClient";

/** Settings — appearance tab. Theme, stored per device. */
export default function OrgSiteAppearanceSettingsRoute() {
  return <AppearanceSettingsClient />;
}

import { ProfileSettingsClient } from "@/components/settings/ProfileSettingsClient";

/** Settings — profile tab. Read-only for a platform account; see the component for why. */
export default function SuperProfileSettingsRoute() {
  return <ProfileSettingsClient />;
}

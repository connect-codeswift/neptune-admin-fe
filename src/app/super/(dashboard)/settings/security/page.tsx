import { SecuritySettingsClient } from "@/components/settings/SecuritySettingsClient";

/** Settings — security tab. Password change via emailed OTP for platform accounts. */
export default function SuperSecuritySettingsRoute() {
  return <SecuritySettingsClient />;
}
